.build/
.swiftpm/
DerivedData/
*.xcodeproj/
*.xcworkspace/
dist/
*.dmg
.DS_Store
xcuserdata/
*.p12
sparkle_private_key*
build/

# Claude Code project rules and hooks are committed so the review bot and every clone share
# them; only the per-machine files stay local.
.claude/settings.local.json
CLAUDE.local.md
