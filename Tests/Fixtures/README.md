# Test fixtures

- `test-3s.mp4` — 3 s, 640×360, 30 fps H.264 `testsrc2` pattern with a 440 Hz sine audio track.
  Regenerate with `Scripts/make-fixture-video.sh` (requires ffmpeg).

Telemetry fixtures (CSV, GPX, …) are added from milestone M1 onward.
