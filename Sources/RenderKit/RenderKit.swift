import ProjectModel
import TelemetryKit

/// Overlay rendering (Core Graphics) and frame compositing. Filled in during milestone M2/M3.
public enum RenderKitInfo {
    public static let name = "RenderKit"
}
