import TelemetryKit

/// File-format importers (RaceRender CSV, GPX, TCX, NMEA, VBO, FIT, generic CSV).
/// Filled in during milestone M1 and M6.
public enum ImportersInfo {
    public static let name = "Importers"
}
